[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not integration"
markers =
    unit: Unit tests that run fast with in-memory database
    integration: Integration tests that require real database
    performance: Performance and scalability tests
